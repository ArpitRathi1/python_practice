# Write a python program to give character of a ASCII number accepted by the user.

Ascii=int(input("Enter Ascii code:"))
character=chr(Ascii)
print("Character of {a} is = {c}".format(a=Ascii,c=character))
